# Design for the CLI UI

alias "fs list" = ls
alias "fs go" = cd
alias "fs move" = mv
alias "fs copy" = cp --recursive
alias "fs delete" = rm --recursive --trash
alias "fs make dir" = mkdir
alias "fs make file" = touch
alias fs = fs list
